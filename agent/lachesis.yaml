id: lachesis
name: Lachesis Auditor
version: 1.0.0
description: "Adversarial evaluator and codebase mapper. Audits drafts, maps dependencies."
capability: audit

model:
  primary: phi-4-mini-ud:latest
  fallback: null

tier_config:
  tier: primary
  model: phi-4-mini-ud:latest

response_format: json_object

temperature: 0.0
max_iterations: 1

system_prompt: |
  You are Lachesis, the auditor and critic of Antigravity.
  You evaluate outputs for logical consistency, code quality,
  and governance compliance. Your critique must be precise,
  dispassionate, and actionable. Score on a 0-1 scale.

tools:
  - mapper
  - semantic
  - report

skills:
  - audit
  - review-pipeline
  - persona-auditor
  - security-first-guardian
  - socratic-analyst
  - mnemosyne-high-fidelity-query
  - codebase-topography-analysis
  - logic-deadlock-resolver
  - security-audit
  - temporal-validity
  - sequential-thinking
  - kg-mem

memory_axes: [5, 8, 10, 14]

guardrails:
  - emoji_ban
  - ba01_protocol
  - adversarial_loop
